import { beforeEach, describe, expect, test } from "vitest";

beforeEach(() => {
  //
});

describe("index", () => {
  test("index", async () => {
    expect("developer to write some tests").toEqual("TODO");
  });
});
